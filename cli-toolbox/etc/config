NETWORK="--testnet-magic 8"
NODE_IMAGE="inputoutput/cardano-node:1.30.1"
CONFIG_IMAGE="ssledz/cardano-node-config:alonzo-purple"
WALLETS_DIR=~/.cardano-wallets
SANDBOX_ROOT_DIR=/tmp/cli-toolbox

mkdir -p $SANDBOX_ROOT_DIR
mkdir -p $WALLETS_DIR

sandbox_dir=$SANDBOX_ROOT_DIR